---
description: Diagnose dekko environment/install issues (PATH shadowing, map freshness, MCP registration, plugin, hooks, CLAUDE.md policy block)
allowed-tools: Bash(dekko:*)
---

## Doctor report

!`dekko doctor`

## Your task

The report above was generated programmatically by the tool above — do
NOT re-derive any of it yourself.

1. Relay the table above verbatim to the user.
2. For any row marked `missing`/`stale`, surface the exact fix command
   it names — do not invent your own.
3. Do not attempt to fix anything yourself unless the user asks.
