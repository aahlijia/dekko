---
name: Bug report
about: Something in dekko's map, CLI, or MCP server isn't behaving correctly
title: ''
labels: bug
assignees: ''
---

**dekko version** (`dekko --version`):

**OS**: (macOS / Linux / Windows, and version)

**Install method**: (`uv tool install`, `pip`, `pipx`, local clone + `install.sh`)

**Describe the bug**

A clear description of what went wrong, and what you expected instead.

**To reproduce**

Steps to reproduce the behavior. For parsing/call-resolution bugs, a
small fixture repo or snippet reproducing the issue helps enormously —
attach it or paste the minimal code.

```sh
dekko map ...
dekko query ...
```

**Relevant output**

```
paste any CLI output, MCP error, or traceback here
```

**Additional context**

Anything else that might be relevant (monorepo size, language, whether
`--full` changes the result, etc.).
