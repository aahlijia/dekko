## Summary

<!-- What does this change, and why? One conceptual change per PR. -->

## Checklist

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] `CHANGELOG.md` updated under `[Unreleased]` (for user-visible changes)
- [ ] Related issue linked, if any (`Closes #...`)

## Test plan

<!-- How did you verify this? New/updated tests, manual repro steps, etc. -->
